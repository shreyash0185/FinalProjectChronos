package com.chronos.scheduler_service.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class SchedulerConfig {
    // Future configurations (custom thread pools, feature toggles)
}
